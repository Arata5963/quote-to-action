<% @suggestions.each do |suggestion| %>
  <li class="px-4 py-2 cursor-pointer hover:bg-accent/10 text-sm text-primary/80"
      role="option"
      data-autocomplete-value="<%= suggestion %>">
    <%= suggestion %>
  </li>
<% end %>
