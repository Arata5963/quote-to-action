<%# ページタイトル %>
<div class="mb-8">
  <h1 class="text-4xl font-black text-black mb-2">きっかけ一覧</h1>
  <p class="text-gray-600">みんなのきっかけとアクションプラン</p>
</div>

<%# 検索フォーム %>
<div class="mb-6 bg-white border-4 border-black p-6">
  <%= search_form_for @q, url: posts_path, method: :get, class: "space-y-4" do |f| %>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
      <%# きっかけで検索 %>
      <div>
        <%= f.label :trigger_content_cont, "きっかけで検索", class: "block font-bold text-black mb-2" %>
        <%= f.search_field :trigger_content_cont, 
              placeholder: "例: 読書", 
              class: "w-full px-4 py-2 border-2 border-black focus:outline-none focus:border-blue-600" %>
      </div>

      <%# アクションプランで検索 %>
      <div>
        <%= f.label :action_plan_cont, "アクションプランで検索", class: "block font-bold text-black mb-2" %>
        <%= f.search_field :action_plan_cont, 
              placeholder: "例: 階段を使う", 
              class: "w-full px-4 py-2 border-2 border-black focus:outline-none focus:border-blue-600" %>
      </div>
    </div>

    <%# 検索ボタン %>
    <div class="flex gap-4">
      <%= f.submit "検索", class: "px-6 py-2 bg-black text-white hover:bg-gray-800 transition-colors font-bold cursor-pointer" %>
      <%= link_to "クリア", posts_path, class: "px-6 py-2 bg-white text-black border-2 border-black hover:bg-gray-100 transition-colors font-bold" %>
    </div>
  <% end %>
</div>

<%# 投稿一覧 %>
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
  <% @posts.each do |post| %>
    <div class="bg-white border-4 border-black p-6 hover:shadow-[8px_8px_0px_0px_rgba(0,0,0,1)] transition-all">
      
      <%# 投稿者情報 %>
      <div class="flex items-center gap-3 mb-4">
        <% if post.user.avatar.present? %>
          <%= image_tag post.user.avatar.thumb.url, 
                class: "w-10 h-10 border-2 border-black object-cover", 
                alt: "プロフィール画像" %>
        <% else %>
          <div class="w-10 h-10 bg-gray-200 border-2 border-black flex items-center justify-center">
            <svg class="w-6 h-6 text-gray-400" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
            </svg>
          </div>
        <% end %>
        
        <div>
          <p class="font-bold text-black"><%= post.user.email.split('@').first %></p>
          <p class="text-xs text-gray-600"><%= l(post.created_at, format: :short) %></p>
        </div>
      </div>

      <%# 画像表示 %>
      <% if post.image.present? %>
        <div class="mb-4 border-2 border-black overflow-hidden">
          <%= image_tag post.image.url, 
                class: "w-full h-48 object-cover", 
                alt: "投稿画像" %>
        </div>
      <% end %>

      <%# きっかけ内容 %>
      <div class="mb-3">
        <h3 class="font-bold text-black mb-2 text-lg">📝 きっかけ</h3>
        <p class="text-gray-700"><%= post.trigger_content %></p>
      </div>

      <%# アクションプラン %>
      <div class="mb-4">
        <h3 class="font-bold text-black mb-2 text-lg">🎯 アクションプラン</h3>
        <p class="text-gray-700"><%= post.action_plan %></p>
      </div>

      <%# 達成バッジ、いいねボタン、詳細ボタン %>
      <div class="flex items-center justify-between pt-4 border-t-2 border-black">
        <div class="flex items-center gap-4">
          <%# 達成バッジ %>
          <%= post_badge_tag(post, class: "w-8 h-8") %>
          
          <%# いいねボタン（パーシャル読み込み） %>
          <%= render 'likes/like_button', post: post %>
        </div>

        <%# 詳細ボタン %>
        <%= link_to "詳細を見る", post_path(post), 
              class: "px-4 py-2 bg-black text-white hover:bg-gray-800 transition-colors font-bold" %>
      </div>
    </div>
  <% end %>
</div>

<%# ページネーション %>
<div class="mt-8">
  <%= paginate @posts %>
</div>

<%# 新規投稿ボタン（ログイン時のみ表示） %>
<% if user_signed_in? %>
  <div class="fixed bottom-8 right-8">
    <%= link_to new_post_path, 
          class: "flex items-center justify-center w-16 h-16 bg-black text-white hover:bg-gray-800 transition-colors shadow-lg border-4 border-black hover:shadow-[4px_4px_0px_0px_rgba(0,0,0,1)]" do %>
      <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
      </svg>
    <% end %>
  </div>
<% end %>