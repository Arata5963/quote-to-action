<%# app/views/recommendations/_modal.html.erb %>
<div data-controller="recommendation-modal"
     class="fixed inset-0 z-50 flex items-center justify-center p-4">

  <%# オーバーレイ %>
  <div data-action="click->recommendation-modal#close"
       class="absolute inset-0 bg-black/60 backdrop-blur-sm"></div>

  <%# モーダル本体 %>
  <div class="relative w-full max-w-2xl bg-white rounded-2xl shadow-2xl overflow-hidden">

    <%# 閉じるボタン %>
    <button type="button"
            data-action="click->recommendation-modal#close"
            class="absolute top-4 right-4 p-2 text-primary/60 hover:text-primary hover:bg-accent/10 rounded-full transition-colors cursor-pointer z-10">
      <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/>
      </svg>
    </button>

    <%# ヘッダー %>
    <div class="pt-8 pb-4 px-6 text-center">
      <div class="text-4xl mb-2">🎉</div>
      <h2 class="text-xl font-bold text-primary">達成おめでとう！</h2>
      <p class="text-sm text-primary/70 mt-1">次はこちらもどうですか？</p>
    </div>

    <%# 推薦投稿リスト %>
    <div class="px-6 pb-6 space-y-3 max-h-[60vh] overflow-y-auto">
      <% if recommended_posts.any? %>
        <% recommended_posts.each do |recommended_post| %>
          <div class="bg-cream rounded-xl border border-accent/10 overflow-hidden hover:shadow-lg hover:-translate-y-1 transition-all duration-300">
            <%= link_to post_path(recommended_post), class: "block p-4", data: { action: "click->recommendation-modal#navigate" } do %>
              <div class="flex gap-4">
                <%# サムネイル（左側） %>
                <div class="flex-shrink-0 w-32 h-20 rounded-lg overflow-hidden bg-primary/10">
                  <% if recommended_post.youtube_thumbnail_url.present? %>
                    <%= image_tag recommended_post.youtube_thumbnail_url,
                                  alt: recommended_post.youtube_title,
                                  class: "w-full h-full object-cover" %>
                  <% end %>
                </div>

                <%# コンテンツ（右側） %>
                <div class="flex-1 min-w-0 space-y-2">
                  <%# 動画タイトル %>
                  <p class="text-sm font-bold text-primary line-clamp-2 leading-tight">
                    <%= recommended_post.youtube_title.presence || recommended_post.youtube_url %>
                  </p>

                  <%# チャンネル名 %>
                  <% if recommended_post.youtube_channel_name.present? %>
                    <p class="text-xs text-primary/60">
                      <%= recommended_post.youtube_channel_name %>
                    </p>
                  <% end %>

                  <%# ユーザー情報 %>
                  <div class="flex items-center gap-2 pt-1">
                    <%# ユーザーアイコン %>
                    <div class="w-6 h-6 rounded-full overflow-hidden bg-accent/20 flex-shrink-0">
                      <% if recommended_post.user.avatar.present? %>
                        <%= image_tag recommended_post.user.avatar.url,
                                      alt: display_name(recommended_post.user),
                                      class: "w-full h-full object-cover" %>
                      <% else %>
                        <div class="w-full h-full flex items-center justify-center text-xs text-primary/60">
                          <%= display_name(recommended_post.user).first %>
                        </div>
                      <% end %>
                    </div>

                    <%# ユーザー名 %>
                    <span class="text-xs text-primary/60 truncate">
                      <%= display_name(recommended_post.user) %>
                    </span>
                  </div>

                  <%# アクションプラン %>
                  <div class="pt-2 border-t border-accent/10">
                    <p class="text-xs text-primary/80 line-clamp-2">
                      <span class="font-medium">アクション:</span>
                      <%= recommended_post.action_plan %>
                    </p>
                  </div>
                </div>

                <%# 矢印アイコン（右端） %>
                <div class="flex-shrink-0 self-center text-accent">
                  <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
                  </svg>
                </div>
              </div>
            <% end %>
          </div>
        <% end %>
      <% else %>
        <div class="text-center py-6 text-primary/60">
          <p>おすすめの投稿はまだありません</p>
        </div>
      <% end %>
    </div>
  </div>
</div>
