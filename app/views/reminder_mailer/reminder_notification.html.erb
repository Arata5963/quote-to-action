<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <style>
      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        text-align: center;
        padding: 20px 0;
        border-bottom: 2px solid #f97316;
      }
      .header h1 {
        color: #f97316;
        margin: 0;
        font-size: 24px;
      }
      .content {
        padding: 30px 0;
      }
      .greeting {
        font-size: 18px;
        margin-bottom: 20px;
      }
      .post-card {
        background: #fff7ed;
        border: 1px solid #fed7aa;
        border-radius: 8px;
        padding: 20px;
        margin: 20px 0;
      }
      .post-card h2 {
        color: #ea580c;
        margin: 0 0 15px 0;
        font-size: 16px;
      }
      .trigger-content {
        font-size: 16px;
        font-weight: bold;
        margin-bottom: 10px;
      }
      .action-plan {
        color: #666;
        padding: 10px;
        background: #fff;
        border-radius: 4px;
      }
      .cta-button {
        display: inline-block;
        background: #f97316;
        color: #fff;
        text-decoration: none;
        padding: 12px 24px;
        border-radius: 6px;
        font-weight: bold;
        margin: 20px 0;
      }
      .footer {
        text-align: center;
        color: #888;
        font-size: 12px;
        border-top: 1px solid #eee;
        padding-top: 20px;
        margin-top: 30px;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1><%= t('reminder_mailer.reminder_notification.header') %></h1>
    </div>

    <div class="content">
      <p class="greeting">
        <%= t('reminder_mailer.reminder_notification.greeting') %>
      </p>

      <p><%= t('reminder_mailer.reminder_notification.intro') %></p>

      <div class="post-card">
        <h2><%= t('reminder_mailer.reminder_notification.your_action') %></h2>
        <div class="action-plan">
          <%= @post.action_plan %>
        </div>
      </div>

      <p><%= t('reminder_mailer.reminder_notification.encouragement') %></p>

      <p style="text-align: center;">
        <a href="<%= post_url(@post) %>" class="cta-button">
          <%= t('reminder_mailer.reminder_notification.view_post') %>
        </a>
      </p>
    </div>

    <div class="footer">
      <p><%= t('reminder_mailer.reminder_notification.footer') %></p>
      <p>&copy; mitadake?</p>
    </div>
  </body>
</html>
