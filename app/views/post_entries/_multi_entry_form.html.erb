<%# app/views/post_entries/_multi_entry_form.html.erb %>
<%# 複数エントリー一括追加フォーム %>

<%= form_with url: bulk_create_post_post_entries_path(post), method: :post, local: true,
      data: { controller: "multi-entry-form", multi_entry_form_auto_init_value: "true" } do %>

  <%# === 要約セクション === %>
  <div style="margin-bottom: 24px;">
    <div style="display: flex; align-items: center; gap: 8px; margin-bottom: 12px;">
      <span style="font-size: 18px;">📝</span>
      <h3 style="font-size: 14px; font-weight: 600; color: #374151;">要約</h3>
      <span style="font-size: 12px; color: #9ca3af;">動画の内容を要約</span>
    </div>
    <div data-multi-entry-form-target="keyPointList" style="display: flex; flex-direction: column; gap: 8px;">
      <%# エントリーはJSで動的に追加 %>
    </div>
    <button type="button"
            data-action="multi-entry-form#addEntry"
            data-entry-type="keyPoint"
            style="margin-top: 8px; padding: 6px 12px; font-size: 13px; color: #6b7280; background: none; border: 1px dashed #d1d5db; border-radius: 6px; cursor: pointer;">
      + 要約を追加
    </button>
  </div>

  <%# === 引用セクション === %>
  <div style="margin-bottom: 24px;">
    <div style="display: flex; align-items: center; gap: 8px; margin-bottom: 12px;">
      <span style="font-size: 18px;">💬</span>
      <h3 style="font-size: 14px; font-weight: 600; color: #374151;">引用</h3>
      <span style="font-size: 12px; color: #9ca3af;">響いたフレーズ</span>
    </div>
    <div data-multi-entry-form-target="quoteList" style="display: flex; flex-direction: column; gap: 8px;">
      <%# エントリーはJSで動的に追加 %>
    </div>
    <button type="button"
            data-action="multi-entry-form#addEntry"
            data-entry-type="quote"
            style="margin-top: 8px; padding: 6px 12px; font-size: 13px; color: #6b7280; background: none; border: 1px dashed #d1d5db; border-radius: 6px; cursor: pointer;">
      + 引用を追加
    </button>
  </div>

  <%# === アクションセクション === %>
  <div style="margin-bottom: 24px;">
    <div style="display: flex; align-items: center; gap: 8px; margin-bottom: 12px;">
      <span style="font-size: 18px;">🎯</span>
      <h3 style="font-size: 14px; font-weight: 600; color: #374151;">アクション</h3>
      <span style="font-size: 12px; color: #9ca3af;">具体的にやること</span>
    </div>
    <div data-multi-entry-form-target="actionList" style="display: flex; flex-direction: column; gap: 8px;">
      <%# エントリーはJSで動的に追加 %>
    </div>
    <button type="button"
            data-action="multi-entry-form#addEntry"
            data-entry-type="action"
            style="margin-top: 8px; padding: 6px 12px; font-size: 13px; color: #6b7280; background: none; border: 1px dashed #d1d5db; border-radius: 6px; cursor: pointer;">
      + アクションを追加
    </button>
  </div>

  <%# === 送信ボタン === %>
  <div style="display: flex; justify-content: flex-end; padding-top: 16px; border-top: 1px solid #f3f4f6;">
    <button type="submit"
            style="padding: 10px 20px; font-size: 14px; font-weight: 500; color: white; background: #8B7355; border: none; border-radius: 8px; cursor: pointer;">
      まとめて保存
    </button>
  </div>
<% end %>
