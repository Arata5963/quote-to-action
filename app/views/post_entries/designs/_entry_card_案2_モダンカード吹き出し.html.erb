<%# app/views/post_entries/_entry_card.html.erb %>
<%# 案2: モダンカード吹き出し - 影とグラデーションでリッチに %>

<div id="<%= dom_id(entry) %>" style="display: flex; gap: 12px; padding: 16px 12px; align-items: flex-start;">
  <%# アバター（上揃え） %>
  <div style="flex-shrink: 0;">
    <% if entry.display_anonymous? %>
      <div style="width: 48px; height: 48px; border-radius: 16px; background: linear-gradient(145deg, #a0aec0, #718096); display: flex; align-items: center; justify-content: center; box-shadow: 0 4px 14px rgba(113, 128, 150, 0.4);">
        <svg style="width: 24px; height: 24px; color: white;" viewBox="0 0 24 24" fill="currentColor">
          <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
        </svg>
      </div>
    <% elsif entry.display_avatar.present? %>
      <%= image_tag entry.display_avatar.url, style: "width: 48px; height: 48px; border-radius: 16px; object-fit: cover; box-shadow: 0 4px 14px rgba(0,0,0,0.15);" %>
    <% else %>
      <div style="width: 48px; height: 48px; border-radius: 16px; background: linear-gradient(145deg, #667eea, #764ba2); display: flex; align-items: center; justify-content: center; color: white; font-weight: 700; font-size: 20px; box-shadow: 0 4px 14px rgba(102, 126, 234, 0.4);">
        <%= entry.display_user_name&.first&.upcase || '?' %>
      </div>
    <% end %>
  </div>

  <%# 吹き出し %>
  <div style="flex: 1; min-width: 0; position: relative;">
    <%# しっぽ（三角形） %>
    <div style="position: absolute; left: -10px; top: 16px;">
      <div style="width: 0; height: 0; border-top: 10px solid transparent; border-bottom: 10px solid transparent; border-right: 10px solid <%= entry.achieved? ? '#e6ffed' : '#fff' %>; filter: drop-shadow(-2px 0 1px rgba(0,0,0,0.04));"></div>
    </div>

    <div style="background: <%= entry.achieved? ? 'linear-gradient(135deg, #e6ffed, #d4f5e0)' : 'linear-gradient(135deg, #ffffff, #fafafa)' %>; border-radius: 20px; border-top-left-radius: 6px; padding: 14px 18px; box-shadow: 0 4px 20px rgba(0,0,0,0.08), 0 1px 3px rgba(0,0,0,0.04);">

      <%# ヘッダー: 名前 + メニュー %>
      <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 8px;">
        <div style="display: flex; align-items: center; gap: 8px;">
          <span style="font-weight: 700; font-size: 14px; color: #2d3748;"><%= entry.display_anonymous? ? '匿名' : entry.display_user_name %></span>
          <% if entry.achieved? %>
            <span style="font-size: 10px; font-weight: 600; color: #38a169; background: #c6f6d5; padding: 2px 8px; border-radius: 10px;">達成 ✓</span>
          <% end %>
        </div>
        <% if user_signed_in? && entry.user == current_user %>
          <details style="position: relative;">
            <summary style="list-style: none; cursor: pointer; padding: 4px; opacity: 0.5; transition: opacity 0.2s;" onmouseover="this.style.opacity='1'" onmouseout="this.style.opacity='0.5'">
              <svg style="width: 16px; height: 16px; color: #718096;" viewBox="0 0 24 24" fill="currentColor"><circle cx="12" cy="5" r="2"/><circle cx="12" cy="12" r="2"/><circle cx="12" cy="19" r="2"/></svg>
            </summary>
            <div style="position: absolute; right: 0; top: 100%; margin-top: 4px; background: white; border-radius: 12px; box-shadow: 0 10px 40px rgba(0,0,0,0.15); border: 1px solid #e2e8f0; z-index: 10; overflow: hidden;">
              <%= button_to post_post_entry_path(entry.post, entry), method: :delete, data: { turbo_confirm: "削除しますか？" }, style: "font-size: 13px; color: #e53e3e; background: none; border: none; cursor: pointer; padding: 10px 20px; white-space: nowrap; transition: background 0.2s;", onmouseover: "this.style.background='#fff5f5'", onmouseout: "this.style.background='transparent'" do %>削除する<% end %>
            </div>
          </details>
        <% end %>
      </div>

      <%# 本文 %>
      <p style="font-size: 15px; line-height: 1.6; color: <%= entry.achieved? ? '#276749' : '#1a202c' %>; margin: 0; <%= 'text-decoration: line-through; opacity: 0.6;' if entry.achieved? %>"><%= entry.content %></p>

      <%# フッター %>
      <div style="display: flex; align-items: center; justify-content: space-between; margin-top: 12px;">
        <% if entry.deadline.present? %>
          <span style="font-size: 12px; color: <%= entry.deadline < Date.current && !entry.achieved? ? '#e53e3e' : '#a0aec0' %>;"><%= entry.achieved? ? '🎉' : '📅' %> <%= entry.deadline < Date.current && !entry.achieved? ? '期限超過' : l(entry.deadline, format: :short) %></span>
        <% else %>
          <span></span>
        <% end %>

        <% if user_signed_in? && entry.user == current_user %>
          <%= form_with url: achieve_post_post_entry_path(entry.post, entry), method: :patch, style: "display: inline;" do %>
            <button type="submit" style="font-size: 13px; font-weight: 600; color: white; background: <%= entry.achieved? ? '#a0aec0' : 'linear-gradient(135deg, #667eea, #764ba2)' %>; border: none; cursor: pointer; padding: 6px 16px; border-radius: 20px; box-shadow: 0 2px 8px <%= entry.achieved? ? 'rgba(160,174,192,0.4)' : 'rgba(102,126,234,0.4)' %>; transition: transform 0.2s, box-shadow 0.2s;" onmouseover="this.style.transform='translateY(-1px)'; this.style.boxShadow='0 4px 12px rgba(102,126,234,0.5)'" onmouseout="this.style.transform='translateY(0)'; this.style.boxShadow='0 2px 8px rgba(102,126,234,0.4)'">
              <%= entry.achieved? ? '戻す' : '達成！' %>
            </button>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>
