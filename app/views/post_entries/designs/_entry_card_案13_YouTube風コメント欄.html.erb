<%# app/views/post_entries/_entry_card.html.erb %>
<%# 案13: YouTube風コメント欄 - YouTubeのコメントセクションを再現 %>

<div id="<%= dom_id(entry) %>" style="display: flex; gap: 12px; padding: 12px 16px; align-items: flex-start;">
  <%# アバター %>
  <div style="flex-shrink: 0;">
    <% if entry.display_anonymous? %>
      <div style="width: 40px; height: 40px; border-radius: 50%; background: #606060; display: flex; align-items: center; justify-content: center;">
        <svg style="width: 20px; height: 20px; color: #aaa;" viewBox="0 0 24 24" fill="currentColor">
          <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
        </svg>
      </div>
    <% elsif entry.display_avatar.present? %>
      <%= image_tag entry.display_avatar.url, style: "width: 40px; height: 40px; border-radius: 50%; object-fit: cover;" %>
    <% else %>
      <div style="width: 40px; height: 40px; border-radius: 50%; background: <%= ['#065fd4', '#ff0000', '#ff9000', '#167ac6', '#7c4dff', '#00a5de'][entry.id % 6] %>; display: flex; align-items: center; justify-content: center; color: white; font-weight: 500; font-size: 16px;">
        <%= entry.display_user_name&.first&.upcase || '?' %>
      </div>
    <% end %>
  </div>

  <%# コンテンツ %>
  <div style="flex: 1; min-width: 0;">
    <%# ヘッダー: @ユーザー名 + 時間 %>
    <div style="display: flex; align-items: center; gap: 4px; margin-bottom: 4px;">
      <span style="font-size: 13px; font-weight: 500; color: #0f0f0f;"><%= entry.display_anonymous? ? '@匿名ユーザー' : "@#{entry.display_user_name}" %></span>
      <span style="font-size: 12px; color: #606060;"><%= time_ago_in_words(entry.created_at) %>前</span>
    </div>

    <%# 本文（チェックボックス付き） %>
    <div style="display: flex; align-items: flex-start; gap: 8px;">
      <% if user_signed_in? && entry.user == current_user %>
        <%= form_with url: achieve_post_post_entry_path(entry.post, entry), method: :patch, style: "flex-shrink: 0; margin-top: 2px;" do %>
          <button type="submit" style="width: 16px; height: 16px; border: 1.5px solid #909090; border-radius: 2px; display: flex; align-items: center; justify-content: center; background: white; cursor: pointer; padding: 0;">
            <% if entry.achieved? %>
              <svg style="width: 12px; height: 12px; color: #606060;" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><path d="M5 12l5 5L20 7" stroke-linecap="round" stroke-linejoin="round"/></svg>
            <% end %>
          </button>
        <% end %>
      <% end %>
      <p style="font-size: 14px; line-height: 1.5; color: #0f0f0f; margin: 0; <%= 'text-decoration: line-through; color: #909090;' if entry.achieved? %>"><%= entry.content %></p>
    </div>

    <%# 期限（あれば） %>
    <% if entry.deadline.present? %>
      <p style="font-size: 12px; color: <%= entry.deadline < Date.current && !entry.achieved? ? '#cc0000' : '#606060' %>; margin: 8px 0 0 <%= user_signed_in? && entry.user == current_user ? '24px' : '0' %>;">
        <%= entry.deadline < Date.current && !entry.achieved? ? '⚠️ 期限超過' : "📅 #{l(entry.deadline, format: :short)}" %>
      </p>
    <% end %>

    <%# 削除メニュー %>
    <% if user_signed_in? && entry.user == current_user %>
      <div style="margin-top: 4px; margin-left: 24px;">
        <details style="position: relative; display: inline-block;">
          <summary style="list-style: none; cursor: pointer; font-size: 12px; color: #606060; padding: 4px 0;" onmouseover="this.style.color='#0f0f0f'" onmouseout="this.style.color='#606060'">
            その他
          </summary>
          <div style="position: absolute; left: 0; top: 100%; margin-top: 4px; background: white; border-radius: 8px; box-shadow: 0 4px 16px rgba(0,0,0,0.14); z-index: 10; overflow: hidden;">
            <%= button_to post_post_entry_path(entry.post, entry), method: :delete, data: { turbo_confirm: "削除しますか？" }, style: "font-size: 13px; color: #cc0000; background: none; border: none; cursor: pointer; padding: 10px 16px; white-space: nowrap;" do %>削除<% end %>
          </div>
        </details>
      </div>
    <% end %>
  </div>
</div>
