<%# app/views/post_entries/_entry_card.html.erb %>
<%# アクションプランカード（LINE風デザイン） %>

<div id="<%= dom_id(entry) %>" class="flex items-start gap-2.5">
  <%# アバター（吹き出しの上部に合わせる） %>
  <div class="flex-shrink-0 pt-5">
    <% if entry.display_anonymous? %>
      <div class="w-10 h-10 rounded-full bg-gray-200 flex items-center justify-center">
        <svg class="w-5 h-5 text-gray-400" viewBox="0 0 24 24" fill="currentColor">
          <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
        </svg>
      </div>
    <% elsif entry.display_avatar.present? %>
      <%= image_tag entry.display_avatar.url,
            alt: entry.display_user_name,
            class: "w-10 h-10 rounded-full object-cover" %>
    <% else %>
      <div class="w-10 h-10 rounded-full bg-gradient-to-br from-emerald-400 to-teal-500 flex items-center justify-center text-white font-semibold text-sm">
        <%= entry.display_user_name&.first&.upcase || '?' %>
      </div>
    <% end %>
  </div>

  <%# 吹き出しエリア %>
  <div class="flex-1 min-w-0" style="max-width: 85%;">
    <%# 名前 %>
    <div class="text-xs text-gray-500 mb-1 ml-3">
      <%= entry.display_anonymous? ? '匿名' : entry.display_user_name %>
    </div>

    <%# 吹き出し %>
    <div class="relative inline-block max-w-full">
      <%# しっぽ（CSS三角形） %>
      <div class="absolute left-0 top-[12px] -translate-x-full">
        <div class="w-0 h-0"
             style="border-top: 6px solid transparent;
                    border-bottom: 6px solid transparent;
                    border-right: 8px solid white;
                    filter: drop-shadow(-1px 0px 1px rgba(0,0,0,0.05));"></div>
      </div>

      <%# 吹き出し本体 %>
      <div class="bg-white rounded-2xl rounded-tl-md px-4 py-3 shadow-sm border border-gray-100">
        <%# コンテンツ（チェックボックス + テキスト） %>
        <div class="flex items-start gap-3">
          <%# チェックボックス（達成状態） %>
          <% if user_signed_in? && entry.user == current_user %>
            <%= form_with url: achieve_post_post_entry_path(entry.post, entry),
                  method: :patch,
                  data: { turbo_frame: dom_id(entry) },
                  class: "flex-shrink-0 pt-0.5" do %>
              <button type="submit" class="block cursor-pointer group">
                <% if entry.achieved? %>
                  <div class="w-5 h-5 rounded bg-emerald-500 flex items-center justify-center group-hover:bg-emerald-600 transition-colors">
                    <svg class="w-3.5 h-3.5 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/>
                    </svg>
                  </div>
                <% else %>
                  <div class="w-5 h-5 rounded border-2 border-gray-300 group-hover:border-emerald-400 transition-colors"></div>
                <% end %>
              </button>
            <% end %>
          <% else %>
            <%# 他人の投稿は読み取り専用 %>
            <div class="flex-shrink-0 pt-0.5">
              <% if entry.achieved? %>
                <div class="w-5 h-5 rounded bg-emerald-500 flex items-center justify-center">
                  <svg class="w-3.5 h-3.5 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/>
                  </svg>
                </div>
              <% else %>
                <div class="w-5 h-5 rounded border-2 border-gray-200"></div>
              <% end %>
            </div>
          <% end %>

          <%# テキスト %>
          <div class="flex-1 min-w-0">
            <p class="text-[15px] leading-relaxed text-gray-800 whitespace-pre-line <%= 'line-through text-gray-400' if entry.achieved? %>"><%= entry.content %></p>

            <%# 期日（ある場合のみ） %>
            <% if entry.deadline.present? %>
              <div class="mt-2">
                <% if entry.achieved? %>
                  <span class="text-xs text-gray-400">
                    📅 <%= l(entry.deadline, format: :short) %>
                  </span>
                <% elsif entry.deadline < Date.current %>
                  <span class="text-xs text-red-500 font-medium">
                    ⚠️ 期限超過
                  </span>
                <% elsif entry.deadline <= Date.current + 3.days %>
                  <span class="text-xs text-orange-500 font-medium">
                    ⏰ あと<%= (entry.deadline - Date.current).to_i %>日
                  </span>
                <% else %>
                  <span class="text-xs text-gray-500">
                    📅 <%= l(entry.deadline, format: :short) %>
                  </span>
                <% end %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <%# 削除ボタン（自分の投稿のみ、吹き出しの下に控えめに） %>
    <% if user_signed_in? && entry.user == current_user %>
      <div class="mt-1 ml-3">
        <%= button_to post_post_entry_path(entry.post, entry),
              method: :delete,
              data: { turbo_confirm: "このアクションプランを削除しますか？" },
              class: "text-xs text-gray-400 hover:text-red-500 cursor-pointer transition-colors" do %>
          削除
        <% end %>
      </div>
    <% end %>
  </div>
</div>
