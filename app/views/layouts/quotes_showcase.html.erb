<!DOCTYPE html>
<html lang="ja">
<head>
  <title><%= content_for(:title) || "引用 - mitadake?" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no">
  <meta name="theme-color" content="#0a0a0a">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

  <style>
    /* ===== 引用ショーケース専用スタイル ===== */
    :root {
      --quote-bg-start: #0a0a0a;
      --quote-bg-end: #1a1a2e;
      --quote-accent: #f59e0b;
      --quote-text: #ffffff;
      --quote-text-muted: rgba(255, 255, 255, 0.6);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html, body {
      height: 100%;
      overflow: hidden;
      font-family: "Hiragino Sans", "Hiragino Kaku Gothic ProN", "Yu Gothic", "Meiryo", sans-serif;
    }

    .quotes-showcase {
      position: fixed;
      inset: 0;
      background: linear-gradient(135deg, var(--quote-bg-start) 0%, var(--quote-bg-end) 100%);
      overflow: hidden;
    }

    /* 背景アニメーション */
    .quotes-bg-animation {
      position: absolute;
      inset: 0;
      overflow: hidden;
      pointer-events: none;
    }

    .quotes-bg-orb {
      position: absolute;
      border-radius: 50%;
      filter: blur(80px);
      opacity: 0.15;
      animation: float-orb 20s ease-in-out infinite;
    }

    .quotes-bg-orb:nth-child(1) {
      width: 400px;
      height: 400px;
      background: #8b5cf6;
      top: -100px;
      left: -100px;
      animation-delay: 0s;
    }

    .quotes-bg-orb:nth-child(2) {
      width: 300px;
      height: 300px;
      background: #f59e0b;
      bottom: -50px;
      right: -50px;
      animation-delay: -5s;
    }

    .quotes-bg-orb:nth-child(3) {
      width: 250px;
      height: 250px;
      background: #06b6d4;
      top: 50%;
      left: 50%;
      animation-delay: -10s;
    }

    @keyframes float-orb {
      0%, 100% { transform: translate(0, 0) scale(1); }
      25% { transform: translate(50px, -30px) scale(1.1); }
      50% { transform: translate(-20px, 50px) scale(0.9); }
      75% { transform: translate(-50px, -20px) scale(1.05); }
    }

    /* ヘッダー */
    .quotes-header {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      padding: 20px 24px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      z-index: 100;
      background: linear-gradient(to bottom, rgba(0,0,0,0.5) 0%, transparent 100%);
    }

    .quotes-back-btn {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      color: var(--quote-text-muted);
      text-decoration: none;
      font-size: 14px;
      padding: 8px 12px;
      border-radius: 8px;
      transition: all 0.3s ease;
    }

    .quotes-back-btn:hover {
      color: var(--quote-text);
      background: rgba(255, 255, 255, 0.1);
    }

    .quotes-video-info {
      display: flex;
      align-items: center;
      gap: 12px;
      max-width: 60%;
    }

    .quotes-video-thumbnail {
      width: 48px;
      height: 27px;
      border-radius: 4px;
      object-fit: cover;
      opacity: 0.8;
    }

    .quotes-video-title {
      font-size: 13px;
      color: var(--quote-text-muted);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    /* メインコンテンツ */
    .quotes-main {
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 100px 24px 120px;
      z-index: 1;
    }

    /* 引用コンテナ */
    .quotes-container {
      width: 100%;
      max-width: 800px;
      min-height: 400px;
      text-align: center;
      position: relative;
    }

    /* 引用カード */
    .quote-card {
      position: absolute;
      inset: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      opacity: 0;
      transform: translateY(40px) scale(0.95);
      transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
      pointer-events: none;
    }

    .quote-card.active {
      opacity: 1;
      transform: translateY(0) scale(1);
      pointer-events: auto;
    }

    .quote-card.exit-up {
      opacity: 0;
      transform: translateY(-60px) scale(0.9);
    }

    .quote-card.exit-down {
      opacity: 0;
      transform: translateY(60px) scale(0.9);
    }

    /* 引用マーク */
    .quote-mark {
      font-size: 120px;
      line-height: 1;
      color: var(--quote-accent);
      opacity: 0.3;
      font-family: Georgia, serif;
      margin-bottom: -40px;
      animation: pulse-glow 3s ease-in-out infinite;
    }

    @keyframes pulse-glow {
      0%, 100% { opacity: 0.3; transform: scale(1); }
      50% { opacity: 0.5; transform: scale(1.05); }
    }

    /* 引用テキスト */
    .quote-text {
      font-size: clamp(24px, 5vw, 42px);
      font-weight: 500;
      color: var(--quote-text);
      line-height: 1.6;
      letter-spacing: 0.02em;
      margin-bottom: 32px;
      position: relative;
    }

    .quote-text-inner {
      display: inline;
    }

    /* タイプライター効果用 */
    .quote-char {
      opacity: 0;
      display: inline;
      animation: char-appear 0.05s ease forwards;
    }

    @keyframes char-appear {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* カーソル */
    .quote-cursor {
      display: inline-block;
      width: 3px;
      height: 1em;
      background: var(--quote-accent);
      margin-left: 4px;
      animation: cursor-blink 0.8s ease-in-out infinite;
      vertical-align: text-bottom;
    }

    @keyframes cursor-blink {
      0%, 50% { opacity: 1; }
      51%, 100% { opacity: 0; }
    }

    .quote-cursor.hidden {
      display: none;
    }

    /* 投稿者情報 */
    .quote-author {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 12px;
      opacity: 0;
      transform: translateY(20px);
      transition: all 0.6s ease 0.3s;
    }

    .quote-card.active .quote-author.visible {
      opacity: 1;
      transform: translateY(0);
    }

    .quote-author-avatar {
      width: 36px;
      height: 36px;
      border-radius: 50%;
      object-fit: cover;
      border: 2px solid rgba(255, 255, 255, 0.2);
    }

    .quote-author-avatar-placeholder {
      width: 36px;
      height: 36px;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.1);
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .quote-author-name {
      font-size: 14px;
      color: var(--quote-text-muted);
    }

    /* 空状態 */
    .quotes-empty {
      text-align: center;
      color: var(--quote-text-muted);
    }

    .quotes-empty-icon {
      font-size: 64px;
      margin-bottom: 24px;
      opacity: 0.5;
    }

    .quotes-empty-text {
      font-size: 18px;
      margin-bottom: 32px;
    }

    .quotes-empty-btn {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 12px 24px;
      background: rgba(255, 255, 255, 0.1);
      color: var(--quote-text);
      text-decoration: none;
      border-radius: 8px;
      font-size: 14px;
      transition: all 0.3s ease;
    }

    .quotes-empty-btn:hover {
      background: rgba(255, 255, 255, 0.2);
    }

    /* フッター（ナビゲーション） */
    .quotes-footer {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      padding: 24px;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 16px;
      z-index: 100;
      background: linear-gradient(to top, rgba(0,0,0,0.5) 0%, transparent 100%);
    }

    /* プログレスドット */
    .quotes-progress {
      display: flex;
      gap: 8px;
    }

    .quotes-progress-dot {
      display: block;
      width: 12px;
      height: 12px;
      padding: 0;
      border: none;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.3);
      cursor: pointer;
      transition: all 0.3s ease;
      -webkit-appearance: none;
      appearance: none;
      pointer-events: auto;
    }

    .quotes-progress-dot.active {
      background: var(--quote-accent);
      transform: scale(1.3);
    }

    .quotes-progress-dot:hover:not(.active) {
      background: rgba(255, 255, 255, 0.5);
    }

    /* ナビボタン */
    .quotes-nav {
      display: flex;
      align-items: center;
      gap: 24px;
    }

    .quotes-nav-btn {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 48px;
      height: 48px;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.1);
      color: var(--quote-text);
      border: none;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .quotes-nav-btn:hover:not(:disabled) {
      background: rgba(255, 255, 255, 0.2);
      transform: scale(1.1);
    }

    .quotes-nav-btn:disabled {
      opacity: 0.3;
      cursor: not-allowed;
    }

    .quotes-nav-btn svg {
      width: 24px;
      height: 24px;
    }

    /* オートプレイ */
    .quotes-autoplay {
      display: flex;
      align-items: center;
      gap: 8px;
      color: var(--quote-text-muted);
      font-size: 13px;
    }

    .quotes-autoplay-toggle {
      position: relative;
      width: 40px;
      height: 22px;
      padding: 0;
      border: none;
      background: rgba(255, 255, 255, 0.2);
      border-radius: 11px;
      cursor: pointer;
      transition: all 0.3s ease;
      -webkit-appearance: none;
      appearance: none;
    }

    .quotes-autoplay-toggle.active {
      background: var(--quote-accent);
    }

    .quotes-autoplay-toggle::after {
      content: '';
      position: absolute;
      top: 2px;
      left: 2px;
      width: 18px;
      height: 18px;
      background: white;
      border-radius: 50%;
      transition: all 0.3s ease;
    }

    .quotes-autoplay-toggle.active::after {
      left: 20px;
    }

    /* カウンター */
    .quotes-counter {
      font-size: 13px;
      color: var(--quote-text-muted);
      font-variant-numeric: tabular-nums;
    }

    /* レスポンシブ */
    @media (max-width: 640px) {
      .quotes-header {
        padding: 16px;
      }

      .quotes-video-info {
        display: none;
      }

      .quote-mark {
        font-size: 80px;
        margin-bottom: -30px;
      }

      .quotes-footer {
        padding: 16px;
      }

      .quotes-nav-btn {
        width: 40px;
        height: 40px;
      }
    }
  </style>
</head>
<body>
  <%= yield %>
</body>
</html>
