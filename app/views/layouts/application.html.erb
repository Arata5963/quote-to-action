<!DOCTYPE html>
<html lang="ja">

  <head>
    <%= display_meta_tags(default_meta_tags) %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%# Tailwind を先に、Application を後に %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>

    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-109R2FXJBS"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-109R2FXJBS');
    </script>
  </head>

  <body class="min-h-screen bg-cream flex flex-col">
    <%= render "shared/header" %>
    <%= render "shared/flash_messages" %>


    <main class="flex-1 pb-20">
      <%= yield %>
    </main>

    <%= render "shared/bottom_nav" %>

    <%# レコメンドモーダル用コンテナ %>
    <div id="recommendation_modal"></div>
  </body>
</html>
