<nav aria-label="Global" class="hidden md:block">
  <ul class="flex items-center gap-6 text-sm">
    <% if user_signed_in? %>
      <li>
        <%= link_to "新規投稿", new_post_path, class: "text-gray-500 transition hover:text-gray-500/75" %>
      </li>
      <li>
        <%= link_to "投稿一覧", posts_path, class: "text-gray-500 transition hover:text-gray-500/75" %>
      </li>

    <% else %>
      <li>
        <%= link_to "ログイン", new_user_session_path, class: "text-gray-500 transition hover:text-gray-500/75" %>
      </li>
    <% end %>
  </ul>
</nav>